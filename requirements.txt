# Requisitos do Sistema de Gerenciamento de Entregas
# 
# NOTA: Python 3.7+ já inclui tkinter e sqlite3
# Este arquivo é para referência futura caso seja necessário adicionar dependências

# Dependências atuais (já incluídas no Python):
# - tkinter (interface gráfica)
# - sqlite3 (banco de dados)
# - datetime (manipulação de datas)
# - os (manipulação de arquivos)

# Dependências futuras sugeridas para melhorias:
# openpyxl>=3.0.0          # Para exportação para Excel
# reportlab>=3.6.0          # Para geração de PDFs
# pillow>=9.0.0             # Para manipulação de imagens
# matplotlib>=3.5.0         # Para gráficos
# pandas>=1.4.0             # Para análise de dados avançada
# psycopg2-binary>=2.9.0    # Para PostgreSQL (multiusuário)
# pymysql>=1.0.0            # Para MySQL (multiusuário)
# flask>=2.2.0              # Para versão web (futuro)

# Para instalar dependências futuras:
# pip install -r requirements.txt --break-system-packages
